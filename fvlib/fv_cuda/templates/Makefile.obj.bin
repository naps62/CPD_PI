ROOTD	=	../..

include $(ROOTD)/conf/config.mk

BIN		=	

include $(ROOTD)/conf/simplebin.mk

$(ROOTD)/$(BIND)/$(BIN):	$(OBJS)
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) $(OUTPUT_OPTION)

all:	$(BIN:%=$(ROOTD)/$(BIND)/%)

clean:
	$(RM) $(BIN:%=$(ROOTD)/$(BIND)/%)
